# 오늘 뭐 입었지? 백엔드 설계 문서 (비개발자용)

> 이 문서는 다시 봐도 이해할 수 있도록, 어려운 용어는 최대한 쉬운 말로 풀어 썼습니다.
> 기준 시점: `docs/ROADMAP.md` 기준 **Phase 2까지 완료**(Task 001~010), **Phase 3(홈·옷장·캘린더·통계·마이 실제 화면 기능, Task 011~016)는 아직 착수 전**입니다.

## 1. 한눈에 보는 구조

이 앱은 자체 서버를 두지 않고, **Supabase**라는 서비스 하나가 "로그인 처리 + 데이터 저장소 + 사진 저장소" 세 가지 역할을 모두 대신해 줍니다.

데이터가 흘러가는 순서는 대략 이렇습니다.

1. **로그인**: 사용자가 이메일/비밀번호 또는 구글 계정으로 로그인하면, Supabase가 신원을 확인하고 브라우저에 "세션 쿠키(로그인 상태를 기억하는 작은 열쇠)"를 발급합니다.
2. **매 요청마다 문지기 확인**: 사용자가 어떤 페이지로 이동할 때마다 `proxy.ts`(Next.js가 요청을 가로채 먼저 검사하는 문지기 코드)가 실행됩니다. 이 문지기는 요청 경로가 `/auth`로 시작하는 경우(로그인/회원가입/비밀번호 재설정 화면)만 로그인 없이 통과시키고, **그 외 모든 경로는 로그인 세션이 없으면 `/auth/login`으로 돌려보냅니다.**
3. **화면에서 데이터 조회 요청**: 로그인이 확인되면, 서버 컴포넌트(화면을 서버에서 미리 그려주는 코드)가 `lib/queries/` 안의 조회 함수를 통해 Supabase 데이터베이스에 "내 데이터를 보여줘"라고 요청합니다.
4. **보안 정책(RLS) 확인**: 데이터베이스는 요청을 받으면 "이 요청을 보낸 사람이 정말 이 데이터의 주인인가"를 자동으로 확인합니다(3절에서 자세히 설명). 주인이 아니면 아예 결과가 0건으로 나옵니다.
5. **데이터 반환 및 화면 표시**: 통과된 데이터만 화면에 표시됩니다.
6. **저장/수정/삭제**: 사용자가 사진을 올리거나 옷을 등록하는 것 같은 "쓰기" 작업은 `app/*/actions.ts`에 정의된 Server Action(서버에서만 실행되는 저장 담당 함수)을 거칩니다. **다만 지금은 이 함수들이 "입력값이 맞는지 검사"와 "로그인했는지 확인"까지만 하고, 실제로 데이터베이스에 저장하는 부분은 아직 비어 있습니다.** (4절에서 자세히 설명)

정리하면, **"조회(읽기)" 통로는 이미 뚫려 있고, "저장(쓰기)" 통로는 골격만 세워진 상태**입니다.

## 2. 데이터는 어디에 저장되나요? (테이블 소개)

테이블(엑셀 시트처럼 데이터를 행/열로 정리해 두는 것)은 현재 3개가 만들어져 있습니다. 위치는 `supabase/migrations/20260820083353_create_outfit_tables.sql`입니다.

### outfits — 하루하루의 "오늘 착장" 기록을 저장하는 시트

- **저장하는 정보**: 기록 날짜(`record_date`), 대표 사진 경로(`photo_url`), 메모(`memo`, 선택 입력), 이 기록의 주인(`user_id`), 만든 시각(`created_at`)
- **다른 테이블과의 관계**: 한 사람(`user_id`)이 여러 개의 기록을 가질 수 있는 1:N 관계입니다. 단, **같은 사람이 같은 날짜에 2개를 만들 수는 없도록** `UNIQUE(user_id, record_date)` 제약(같은 값의 조합이 중복되지 못하게 막는 규칙)이 걸려 있습니다.
- **왜 이렇게 만들었나**: PRD가 "하루에 대표 착장 1건"을 원칙으로 하기 때문에(F013), 데이터베이스 차원에서부터 중복 기록이 물리적으로 불가능하게 설계했습니다.

### clothing_items — 옷장에 등록된 옷 하나하나를 저장하는 시트

- **저장하는 정보**: 카테고리(`category`: 상의/하의/신발/아우터/기타), 이름(`name`), 사진 경로(`photo_url`), 주인(`user_id`)
- **다른 테이블과의 관계**: 역시 한 사람이 여러 옷을 등록할 수 있는 1:N 관계입니다.
- **왜 이렇게 만들었나**: `category` 컬럼에는 `CHECK` 제약(정해진 값 외에는 저장을 거부하는 규칙)이 걸려 있어 `top/bottom/shoes/outer/etc` 다섯 가지 값 외에는 저장될 수 없습니다. 이 다섯 값은 코드 쪽 상수(`lib/constants/category.ts`)와 반드시 같은 값을 쓰도록 마이그레이션 파일 맨 위에 주석으로 명시되어 있습니다 — 화면에서 쓰는 카테고리 목록과 데이터베이스가 허용하는 카테고리 목록이 어긋나지 않게 하려는 안전장치입니다.

### outfit_items — "오늘 착장"과 "옷장 아이템"을 서로 이어주는 연결 시트

- **저장하는 정보**: 어떤 착장 기록(`outfit_id`)에 어떤 옷 아이템(`clothing_item_id`)이 연결되어 있는지만 담습니다. 이 테이블 자체는 사진이나 이름 같은 정보를 갖지 않습니다.
- **다른 테이블과의 관계**: 착장 1건에 옷 여러 개, 옷 1개가 여러 착장에 등장할 수 있는 **N:M(다대다) 관계**를 표현하는 중간 다리 역할입니다. `ON DELETE CASCADE`(부모 데이터가 지워지면 자식 데이터도 자동으로 함께 지워지는 규칙)가 걸려 있어서, 착장 기록을 지우면 거기 연결된 이 다리 데이터도 자동으로 함께 정리됩니다. 또한 `UNIQUE(outfit_id, clothing_item_id)`로 같은 조합이 중복 저장되는 것도 막습니다.
- **왜 이렇게 만들었나**: "오늘 상의A + 하의B + 신발C를 입었다"처럼 여러 옷을 한 기록에 자유롭게 연결하는 F002 요구사항을 구현하려면 이런 다리 테이블이 필요합니다.

### 인덱스(찾는 속도를 빠르게 해주는 색인)

`outfits(user_id, record_date)`, `clothing_items(user_id, category)`, `outfit_items(outfit_id)`, `outfit_items(clothing_item_id)`에 인덱스가 걸려 있습니다. 캘린더에서 날짜별로 찾거나, 옷장에서 카테고리별로 걸러보거나, 통계에서 아이템별 착용 횟수를 셀 때 이 색인이 사용됩니다.

### Storage 버킷(사진 파일을 담는 창고)

`supabase/migrations/20260820083401_create_storage_buckets.sql`에서 `outfit-photos`(착장 사진용)와 `item-photos`(옷 아이템 사진용) 두 개의 창고가 만들어졌습니다. 사진 파일 자체는 이 창고에 저장되고, 데이터베이스의 `photo_url` 컬럼에는 그 파일이 있는 "주소"만 저장됩니다.

## 3. 내 정보는 안전한가요? (보안 장치)

여러 사람이 같은 앱을 함께 쓰기 때문에, "내가 등록한 옷과 기록이 다른 사람 화면에는 절대 보이면 안 된다"는 것이 가장 기본적인 보안 요구사항입니다. 이를 지켜주는 장치가 두 가지 있습니다.

**① RLS(Row Level Security, 행 단위 보안 — 내 데이터만 나에게 보이게 하는 자물쇠)**

3개 테이블 모두 RLS가 켜져 있고, 각 테이블마다 조회(SELECT)·등록(INSERT)·수정(UPDATE)·삭제(DELETE) 4종류의 자물쇠(정책, Policy)가 걸려 있어 총 12개입니다. 규칙은 단순합니다.

```sql
create policy "Users can view own outfits"
  on public.outfits for select
  using (auth.uid() = user_id);
```

이 코드가 하는 일: 로그인한 사람의 고유번호(`auth.uid()`)와 그 기록의 주인(`user_id`)이 같을 때만 조회를 허용합니다.
왜 이렇게 만들었나: 데이터베이스 자체가 "너는 네 것만 봐야 한다"를 강제하기 때문에, 화면 코드에 실수가 있어도 남의 데이터가 새어 나가지 않습니다.

`outfit_items`(연결 다리 테이블)는 자기 자신에 `user_id`가 없기 때문에, 조금 더 복잡하게 "이 연결이 속한 착장 기록의 주인이 나인가"를 매번 검사합니다(코드에서 `exists (... o.user_id = auth.uid())` 형태로 확인). 등록(INSERT)할 때는 한 걸음 더 나아가 "연결하려는 착장도 내 것, 연결하려는 옷 아이템도 내 것"인지 둘 다 검사합니다. 이렇게 만든 이유는, 남의 착장 기록에 몰래 내 옷을 연결하거나 남의 옷을 내 기록에 끌어다 붙이는 두 가지 시도를 모두 막기 위해서입니다.

**② Storage 접근 정책 — 사진 창고도 사람별로 칸이 나뉘어 있습니다**

사진은 업로드할 때 `{내_고유번호}/{임의의_파일명}.webp` 형태의 경로에 저장됩니다. 아래 정책이 "본인 폴더에만 올리고, 지우고, 바꿀 수 있다"를 강제합니다.

```sql
create policy "Users can upload own bucket objects"
  on storage.objects for insert
  with check (
    bucket_id in ('outfit-photos', 'item-photos')
    and (storage.foldername(name))[1] = auth.uid()::text
  );
```

이 코드가 하는 일: 업로드하려는 파일 경로의 맨 앞 폴더 이름이 내 고유번호와 같을 때만 업로드를 허용합니다.
왜 이렇게 만들었나: 남의 고유번호 폴더에 사진을 몰래 끼워 넣는 것을 막기 위해서입니다.

**주의할 점(정직하게 알려드립니다)**: 두 창고(`outfit-photos`, `item-photos`)는 모두 **공개(public) 버킷**으로 만들어져 있습니다. 즉 사진을 "올리고 지우는" 행위는 본인 폴더로 제한되지만, **사진 주소(URL) 자체를 알고 있는 사람은 로그인 없이도 그 사진을 볼 수 있습니다.** 이는 개인용 옷장 로그 앱 특성상 큰 문제는 아니지만, 사진 URL이 외부에 노출되지 않도록 주의가 필요한 부분이라 명확히 짚어둡니다.

**③ 라우트 보호 — 로그인 안 하면 화면 자체에 못 들어옵니다**

`proxy.ts`가 모든 페이지 요청에 대해 로그인 여부를 먼저 확인합니다. 실제 코드(`lib/supabase/proxy.ts`)를 보면 로그인 없이 접근을 허용하는 경로는 `/auth`로 시작하는 로그인·회원가입·비밀번호 재설정 화면뿐이고, **그 외의 모든 경로(홈, 옷장, 캘린더, 통계, 마이 페이지 포함)는 로그인하지 않으면 자동으로 `/auth/login`으로 돌려보냅니다.**

## 4. 지금까지 만들어진 기능 / 아직 안 만들어진 기능

| 기능                                    | 상태        | 설명                                                                                                                                                                                                                             |
| --------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 데이터베이스 뼈대(테이블·관계·제약조건) | 완료        | `outfits`/`clothing_items`/`outfit_items` 3개 테이블, 관계, 인덱스가 모두 생성됨                                                                                                                                                 |
| RLS 보안 정책 12종                      | 완료        | 3개 테이블 × 4종(조회/등록/수정/삭제) 정책이 모두 적용됨                                                                                                                                                                         |
| Storage 버킷 2종 + 접근 정책 4종        | 완료        | 사진 창고와 "본인 폴더만 접근 가능" 규칙까지 완료                                                                                                                                                                                |
| 프론트-백엔드 연결 클라이언트           | 완료        | `lib/supabase/client.ts`(브라우저용), `server.ts`(서버용), `types.ts`(테이블 구조를 코드가 알 수 있게 자동 생성된 타입)                                                                                                          |
| 조회(읽기) 함수                         | 완료        | `lib/queries/outfits.ts`(날짜별 착장 조회, 월별 기록 날짜 목록), `clothing-items.ts`(옷장 목록 조회), `stats.ts`(아이템별·카테고리별 착용 횟수 집계) — 함수 자체는 실제로 동작하는 수준으로 작성되어 있음                        |
| 저장(쓰기) 기능 — 옷 아이템 등록        | 골격만 있음 | `app/closet/actions.ts`의 `createClothingItem`이 "입력값 검증 + 로그인 확인"까지만 하고, 마지막에 `return { success: false, error: "아직 구현되지 않았습니다" }`로 끝남. 사진 업로드→DB 저장 연결은 Task 012(F003)에서 완성 예정 |
| 저장(쓰기) 기능 — 오늘 착장 기록        | 골격만 있음 | `app/outfits/actions.ts`의 `createOutfit`도 동일한 구조로, 실제 저장 로직은 Task 014(F001, F002)에서 완성 예정                                                                                                                   |
| 화면(홈/옷장/캘린더/통계/마이)          | 미착수      | `app/(tabs)/page.tsx` 등은 "○○ 화면 (Task 0XX에서 구현)"이라는 안내 문구만 있는 빈 페이지 상태                                                                                                                                   |
| 이미지 업로드 공통 컴포넌트             | 완료        | `components/common/image-uploader.tsx` + `lib/storage/upload.ts` — 이미지 축소·WebP 변환·업로드·고아 파일 정리까지 실제로 동작함(화면에 아직 연결만 안 됨)                                                                       |
| 착장 기록/옷 아이템 수정·삭제           | 미착수      | Task 013, 017에서 예정. 현재 Server Action 자체가 없음                                                                                                                                                                           |
| 계정 정보 확인·로그아웃 화면            | 미착수      | 로그아웃 기능 자체(F010)는 이미 있지만, 마이 페이지에서 이를 보여주는 것은 Task 018 예정                                                                                                                                         |

## 5. 요구사항과 비교했을 때

**PRD/ROADMAP에는 있는데 아직 없는 것**

- F001~F009, F011~F013에 해당하는 실제 화면 동작이 전부 없습니다. 다만 이는 "빠뜨린 것"이 아니라 ROADMAP상 Phase 3(다음 단계)에서 순서대로 만들 예정으로 계획되어 있는 부분입니다.

**코드에는 있는데 요구사항에서 근거를 찾지 못한 것(군더더기 후보)**

- `lib/supabase/types.ts`에 `profiles`라는 테이블 타입이 남아 있습니다. 그런데 실제 마이그레이션 파일(`supabase/migrations/*.sql`) 어디에도 `profiles` 테이블을 만드는 코드가 없고, PRD 7절 데이터 모델에도 `profiles`는 없습니다. ROADMAP Task 004의 완료 기준에는 "기존 instruments/profiles 템플릿 제거"라고 적혀 있는데, 실제로는 타입 파일에 흔적이 남아 있는 상태입니다. 코드 어디에서도 `profiles`를 실제로 사용하는 부분은 없었으므로(검색 결과 ROADMAP.md와 types.ts 두 곳에서만 발견), 당장 문제를 일으키진 않지만 **다음 타입 재생성 시 자동으로 정리될 것으로 보이는 잔재**입니다.
- Storage 버킷이 공개(public)로 설정된 점은 3절에서 설명한 대로, PRD에 명시적 근거는 없지만(공개/비공개를 규정하지 않음) 개인용 앱 특성상 치명적 문제는 아닙니다. 다만 "왜 public인지"에 대한 별도 설명은 코드/문서 어디에도 없어 확인이 필요합니다.

**그 외**: 특이사항 없음. 테이블 구조·컬럼·제약조건은 PRD 7절 데이터 모델과 정확히 일치합니다.

## 6. 다음 단계

ROADMAP 기준 다음 실행 작업은 **Task 011(홈 — 오늘 기록 여부 안내)**이며, 이어서 Task 012(옷장 등록), Task 013(옷 아이템 수정·삭제), Task 014(오늘 착장 기록 저장), Task 015(캘린더), Task 016(통계) 순으로 진행될 예정입니다.

이 문서에서 "아직 없음"으로 표시한 항목들은 다음과 같이 채워질 예정입니다.

- **저장(쓰기) 기능 완성**: Task 012에서 `createClothingItem`이, Task 014에서 `createOutfit`이 지금의 "아직 구현되지 않았습니다" 자리에 실제 Storage 업로드 + 데이터베이스 저장 로직으로 교체됩니다.
- **수정·삭제 기능**: Task 013(옷 아이템), Task 017(착장 기록)에서 새로 만들어집니다.
- **화면 5종(홈/옷장/캘린더/통계/마이)**: 지금의 안내 문구만 있는 빈 페이지가 Task 011·012·015·016·018에서 각각 실제 조회 화면으로 교체됩니다.
- **`profiles` 타입 잔재**: 다음 번 `mcp__supabase__generate_typescript_types`(Supabase 데이터베이스 구조를 코드 타입으로 다시 만들어내는 작업) 실행 시 자연스럽게 사라질 가능성이 높으나, 별도 확인이 필요합니다.

---

참고한 파일: `docs/ROADMAP.md`, `docs/PRD.md`, `supabase/migrations/20260820083353_create_outfit_tables.sql`, `supabase/migrations/20260820083401_create_storage_buckets.sql`, `lib/supabase/client.ts`, `lib/supabase/server.ts`, `lib/supabase/proxy.ts`, `lib/supabase/types.ts`, `proxy.ts`, `lib/queries/outfits.ts`, `lib/queries/clothing-items.ts`, `lib/queries/stats.ts`, `app/outfits/actions.ts`, `app/closet/actions.ts`, `lib/storage/upload.ts`, `types/action.ts`, `lib/errors.ts`, `lib/constants/category.ts`, `app/(tabs)/page.tsx`, `app/(tabs)/closet/page.tsx`(모두 `C:\Users\admin\Desktop\workspaces\todays-outfit` 기준 절대경로).
